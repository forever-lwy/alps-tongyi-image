{
  "identifier": "alps-exa-search",
  "api": [
    {
      "url": "https://alps-tongyi-image-kappa.vercel.app/api/generate",
      "name": "searchWeb",
      "description": "Search the web and extract content from results using the Exa Search API",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query string"
          }
        },
        "required": ["query"]
      }
    }
  ],
  "meta": {
    "avatar": "🔍",
    "description": "This plugin uses Exa Search API to search the web intelligently and extract content from results.",
    "tags": ["search", "web", "content", "exa"],
    "title": "Exa Web Search"
  },
  "settings": {
    "type": "object",
    "required": ["EXA_API_KEY"],
    "properties": {
      "EXA_API_KEY": {
        "type": "string",
        "title": "Exa API Key",
        "description": "The API key to use the Exa Search API"
      },
      "EXA_SEARCH_TYPE": {
        "type": "string",
        "title": "搜索类型",
        "description": "选择搜索类型: keyword(关键词搜索)、neural(神经搜索)或auto(自动选择)",
        "enum": ["keyword", "neural", "auto"],
        "default": "keyword"
      },
      "EXA_SEARCH_NUM_RESULTS": {
        "type": "integer",
        "title": "结果数量",
        "description": "返回的搜索结果数量（1-100）",
        "default": 25,
        "minimum": 1,
        "maximum": 100
      },
      "EXA_SEARCH_SUMMARY": {
        "type": "boolean",
        "title": "返回网页摘要",
        "description": "是否返回网页摘要",
        "default": true
      },
      "EXA_SEARCH_TEXT": {
        "type": "boolean",
        "title": "返回网页全文",
        "description": "是否返回网页全文（可能增加响应大小）",
        "default": false
      }
    }
  },
  "version": "1",
  "systemRole": "This plugin uses Exa Search API to search the web intelligently and extract content from results."
}